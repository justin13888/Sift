/* SwiftPM requires a system-library target to have an include directory. The module map
   beside this file points at the generated header in the Rust workspace, which is the one
   artefact both sides compile against. */
